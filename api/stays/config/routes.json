{
  "routes": [
    {
      "method": "GET",
      "path": "/stays",
      "handler": "stays.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/stays/count",
      "handler": "stays.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/stays/:id",
      "handler": "stays.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/stays",
      "handler": "stays.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/stays/:id",
      "handler": "stays.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/stays/:id",
      "handler": "stays.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
